## Summary

<!-- What changes, and why? -->

## Blast radius

<!-- This package is a Composer plugin consumed by ekwi-core (it replaces olvlvl/composer-attribute-collector).
     Say what a consumer sees on the next release:

     - Public API change? (Attributes::find*, the collected attributes contract)
     - Plugin behaviour change a consumer notices at build/dump-autoload time?
     - composer.json change (name, replace, require constraints)?

     "Nothing — internal, docs or CI only" is a perfectly good answer. State it explicitly. -->

## Upstream sync note

<!-- Does this touch a file also owned by upstream olvlvl/composer-attribute-collector (src/, cases/,
     README.md, composer.json)? Such changes become conflicts on the monthly sync — keep them minimal and
     intentional. Leave blank if not applicable. -->

## Checklist

- [ ] PR title follows Conventional Commits (enforced by the Semantic PR Title check)
- [ ] Unit tests added or updated for changed logic
- [ ] For an upstream-sync PR: merge with a **merge commit** (not squash) to preserve the shared ancestor
